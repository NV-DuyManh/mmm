<Page x:Class="Cinema.PagePhim"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="#F4F6F9">

    <Page.Resources>

        <!-- Style chung cho Button -->
        <Style TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== HEADER ===== -->
        <Border Background="White"
                Padding="20"
                CornerRadius="12"
                Margin="0,0,0,20">

            <StackPanel>
                <TextBlock Text="🎬 QUẢN LÝ PHIM"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#2C3E50"/>

                <TextBlock Text="Hệ thống quản lý danh sách phim rạp chiếu"
                           FontSize="14"
                           Foreground="#7F8C8D"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- ===== BUTTON BAR ===== -->
        <Border Grid.Row="1"
                Background="White"
                Padding="15"
                CornerRadius="12"
                Margin="0,0,0,20">

            <StackPanel Orientation="Horizontal">

                <Button Content="➕ Thêm mới"
                        Background="#27AE60"
                        Click="BtnThemMoi_Click"/>

                <Button Content="✏️ Sửa"
                        Background="#F39C12"
                        Click="BtnSua_Click"/>

                <Button Content="🗑 Xóa"
                        Background="#E74C3C"
                        Click="BtnXoa_Click"/>

            </StackPanel>
        </Border>

        <!-- ===== DATA GRID ===== -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="12"
                Padding="15">

            <DataGrid x:Name="dgPhim"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          CanUserAddRows="False"
          AlternatingRowBackground="#F8F9FA"
          RowHeight="40"
          FontSize="14"
          GridLinesVisibility="None"
          HeadersVisibility="Column"
          SelectionMode="Single"
          SelectionUnit="FullRow"
          BorderThickness="0">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Mã phim"
                            Binding="{Binding ma_phim}"
                            Width="80"/>

                    <DataGridTextColumn Header="Tên phim"
                            Binding="{Binding ten_phim}"
                            Width="*"/>

                    <DataGridTextColumn Header="Mã thể loại"
                            Binding="{Binding ma_the_loai}"
                            Width="110"/>

                    <DataGridTextColumn Header="Thời lượng"
                            Binding="{Binding thoi_luong}"
                            Width="110"/>

                    <DataGridTextColumn Header="Khởi chiếu"
                            Binding="{Binding ngay_khoi_chieu, StringFormat=dd/MM/yyyy}"
                            Width="120"/>

                    <DataGridTextColumn Header="Kết thúc"
                            Binding="{Binding ngay_ket_thuc, StringFormat=dd/MM/yyyy}"
                            Width="120"/>

                    <DataGridTextColumn Header="Mô tả"
                            Binding="{Binding mo_ta}"
                            Width="200"/>

                    <DataGridTextColumn Header="Trạng thái"
                            Binding="{Binding trang_thai}"
                            Width="110"/>

                    <DataGridTextColumn Header="Người nhập"
                            Binding="{Binding nguoi_nhap}"
                            Width="100"/>

                </DataGrid.Columns>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Foreground" Value="#2C3E50"/>
                        <Setter Property="Background" Value="#ECF0F1"/>
                        <Setter Property="Height" Value="45"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

            </DataGrid>
        </Border>

    </Grid>
</Page>
